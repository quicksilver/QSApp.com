name: Deploy

on:
  push:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive
    - name: Pull changes
      env:
        SERVER: ${{ secrets.SERVER }}
        PORT: ${{ secrets.PORT }}
        SSH_KEY: ${{secrets.SSH_KEY}}
      run: |
        echo "$SSH_KEY" > key
        ssh "${SERVER}" -p "${PORT}" -i ./key 'git -C www pull --dry-run -vv'
